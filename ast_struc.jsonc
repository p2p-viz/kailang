/**
import utils
import graphics.vk

map [ a -> number, b -> number, c -> number ] = [
  a -> 4,
  b -> 5,
  c -> 9
]

Result = T : E : enum(
  Success(T),
  Error(E)
)

DivisionResult = Result number string

divBy0 = DivisionResult.Error("Divided By 0")

arr [ _0 -> number, _1 -> number, _2 -> number ] = [ 1,2,3 ]


arr [number] = [2, 3, 4, 5]

map = Map.setDefaultFn(map, mp: {
  mp.a + mp.b
})

pub facto = x : if (x==0) (1) (x * facto(x-1))

if (T : (std.boolean : T : T : T))

main = args [string] : IO (
  print "Hello World",
  x <- std.parseNumIO(std.readIO()), // read = (): IO(string) // parseNumIO = IO(string) : IO(number)
  print "Factorial of {x} is {facto x}"
)

readIO = () : IO (
  res = cFnCallIO("readline"),
  str <- parseCStrIO(res),
  str
)
*/
{
    "Result": {
        "type": "function",
        "param": {
            "name": "T",
            "type": "GenericType"
        },
        "returns": {
            "type": "function",
            "param": {
                "name": "E",
                "type": "GenericType"
            },
            "returns": {
                "type": "enum",
                "keys": {
                    "Success": {
                        "type": "T"
                    },
                    "Error": {
                        "type": "E"
                    }
                }
            }
        }
    },
    "facto": {
        "type": "function",
        "param": {
            "name": "x",
            "type": "number"
        },
        "returns": {
            "type": "number"
        },
        "body": {
            "fnCall": {
                "fnName": "if",
                "param": {
                    "fnCall": {
                        "fnName": "==",
                        "param": {
                            "value": "x"
                        }
                    }
                }
            }
        }
    }
}